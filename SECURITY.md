# Security policy

Report suspected security issues privately through GitHub security advisories. Do not publish tenant IDs, user data, access tokens, client secrets, or Terraform state.

Use a dedicated lab tenant and a least-privilege identity. Pull-request checks intentionally run without Entra credentials.
